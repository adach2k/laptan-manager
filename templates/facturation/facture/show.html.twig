{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ facture.numero }}</td>
            </tr>
            <tr>
                <th>MontantTotal</th>
                <td>{{ facture.montantTotal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('facturation_facture_index') }}">back to list</a>

    <a href="{{ path('facturation_facture_edit', {'id': facture.id}) }}">edit</a>

    {{ include('facturation_facture/_delete_form.html.twig') }}
{% endblock %}
