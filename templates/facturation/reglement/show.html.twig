{% extends 'base.html.twig' %}

{% block title %}Reglement{% endblock %}

{% block body %}
    <h1>Reglement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reglement.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ reglement.code }}</td>
            </tr>
            <tr>
                <th>MontantPaye</th>
                <td>{{ reglement.montantPaye }}</td>
            </tr>
            <tr>
                <th>MontantRestant</th>
                <td>{{ reglement.montantRestant }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reglement.etat ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('facturation_reglement_index') }}">back to list</a>

    <a href="{{ path('facturation_reglement_edit', {'id': reglement.id}) }}">edit</a>

    {{ include('facturation_reglement/_delete_form.html.twig') }}
{% endblock %}
