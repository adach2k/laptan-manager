{% extends 'base.html.twig' %}

{% block title %}Building{% endblock %}

{% block body %}
    <h1>Building</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ building.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ building.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('place_building_index') }}">back to list</a>

    <a href="{{ path('place_building_edit', {'id': building.id}) }}">edit</a>

    {{ include('place/building/_delete_form.html.twig') }}
{% endblock %}
