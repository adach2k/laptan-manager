{% extends 'base.html.twig' %}

{% block title %}Bed{% endblock %}

{% block body %}
    <h1>Bed</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bed.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ bed.numero }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ bed.disponible ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('place_bed_index') }}">back to list</a>

    <a href="{{ path('place_bed_edit', {'id': bed.id}) }}">edit</a>

    {{ include('place/bed/_delete_form.html.twig') }}
{% endblock %}
